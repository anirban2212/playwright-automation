{
  "name": "playwright-automation",
  "version": "1.0.0",
  "description": "automation framework using playwright and cucumber",
  "author": "Anirban Nag",
  "scripts": {
    "report": "node index.js",
    "install-playwright": "npx playwright install",
    "tag.sauceDemo": "npm run install-playwright && cucumber-js --tags \"@UI\" --config=cucumber.json && npm run report",
    "tag.automationStore": "npm run install-playwright && cucumber-js --tags \"@UI\" --config=cucumber.json && npm run report",
    "test.sauceDemo": "cross-env environment=saucedemo npm run tag.sauceDemo",
    "test.automationStore": "cross-env environment=automationStore npm run tag.automationStore"
  },
  "dependencies": {
    "@cucumber/cucumber": "^10.0.0",
    "@playwright/test": "^1.57.0",
    "dotenv": "^16.4.7",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2",
    "cross-env": "^7.0.3",
    "playwright": "^1.57.0",
    "cucumber-html-reporter": "^7.2.0",
    "pino": "^9.7.0",
    "pino-pretty": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.10",
    "@types/cucumber": "6.0.1"
  }
}